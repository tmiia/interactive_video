import Sequence from "../sequence.js";

export default class Title extends Sequence {
  constructor(context, rank) {
    super(context, rank);
  }

  play(){
    super.play()
  }

  stop(){
    super.stop()
  }
}
